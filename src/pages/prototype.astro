---
import Layout from "@layouts/Layout.astro";
import P from "@components/P.astro";
import Strong from "@components/Strong.astro";
import ProtoypeCard from "@components/CarCard.astro";

import { cards } from "lib/prototype";
import { Image } from "astro:assets";

import prototype from "@assets/prototype.jpg";
---

<Layout title="Heritage | Calandra Automobili">
  <section
    class="relative flex flex-col justify-center items-center w-full h-[80vh]"
  >
    <figure class="absolute flex inset-0 bg-black">
      <video
        class="w-full h-full object-cover"
        src="/prototype.mp4"
        playsinline
        muted
        autoplay
        loop></video>
      <span class="absolute inset-0 bg-black/60"></span>
    </figure>

    <header class="flex flex-col gap-8 p-6 z-10">
      <h1
        class="text-4xl font-normal leading-tight text-center text-gray-300 lg:text-6xl"
      >
        Calandra <Strong green>P001</Strong>
      </h1>

      <P>
        After more than 14 year of design, development and engineering work, we
        can finally say that the Calandra P001 is ready.
      </P>
    </header>
  </section>

  <section class="flex flex-col items-center w-full max-w-5xl px-8 pt-10 md:pt-32">
    <ul class="flex flex-wrap justify-center w-full">
      {
        cards.map((card) => (
          <li class="self-stretch w-full md:w-1/3 p-2">
            <ProtoypeCard {...card} />
            </li>
        ))
      }
    </ul>
  </section>
  
  <section class="relative my-20">
    <Image src={prototype} alt="prototype" width="1400" height="766" class="scale-150 md:scale-100"/>
  </section>
</Layout>
